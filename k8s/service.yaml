apiVersion: v1
kind: Service
metadata:
  name: credit-score
  namespace: default
spec:
  selector:
    app: credit-score
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 8000
    nodePort: 32001
